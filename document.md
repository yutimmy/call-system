# 點名單網頁－需求與開發規格

## 1. 專案目標

建立一套前後端分離的點名單網頁，讓使用者可以：

- 依人員分類查看與點名。
- 快速登記人員事故狀態與事故時間。
- 即時查看各事故類型的人數。
- 依事故人數由多至少排列，多的顯示在上方。
- 自動計算應到、事故合計與實到人數。
- 使用 JSON 檔匯入人員名單。
- 使用簡潔、大方、容易操作的扁平化介面。

## 2. 名詞與統計規則

### 2.1 人員分類

系統預設支援以下六種分類：

1. 入伍生
2. 學生幹部
3. 一年級
4. 二年級
5. 三年級
6. 四年級

使用者可以選取單一分類，也可以選取「全部」。所有統計數字都以目前選取的分類為範圍。

### 2.2 首頁統計

首頁需顯示：

```text
應到：XX 員

事故
醫護所：XX 員
轉診：XX 員
全休：XX 員

合計：XX 員事故
實到：XX 員
```

計算方式：

- **應到**：目前選取分類中的啟用人員總數。
- **各類事故人數**：目前仍在有效事故時間內，且屬於該事故類型的人數。
- **事故合計**：目前有有效事故紀錄的「不重複人員數」。
- **實到**：`應到 - 事故合計`。
- 同一人同一時間只能有一筆有效事故，避免重複扣除。
- 事故區塊依各類事故人數由多至少排序；同人數時依「醫護所、轉診、全休」的固定順序排列，避免畫面一直跳動。

> 若未來有「公差、休假、住院、遲到」等類型，可以新增事故類型設定，不需要修改人員資料格式。

## 3. 功能需求

### 3.1 人員名單匯入

- 由管理者選取 `.json` 檔案匯入。
- 匯入前先驗證檔案格式、必要欄位、分類是否合法，以及人員編號是否重複。
- 匯入結果要顯示成功筆數、失敗筆數及錯誤原因。
- 同一人員編號再次匯入時，預設更新既有人員資料，不重複建立。
- 人員可設為停用；停用後不列入應到，但歷史事故紀錄保留。

建議 JSON 格式：

```json
{
  "people": [
    {
      "id": "A001",
      "name": "王小明",
      "category": "一年級",
      "enabled": true
    },
    {
      "id": "A002",
      "name": "陳小華",
      "category": "學生幹部",
      "enabled": true
    }
  ]
}
```

欄位說明：

| 欄位 | 型別 | 必填 | 說明 |
| --- | --- | --- | --- |
| `id` | 字串 | 是 | 不可重複的人員編號 |
| `name` | 字串 | 是 | 人員姓名 |
| `category` | 字串 | 是 | 必須是系統支援的分類之一 |
| `enabled` | 布林值 | 否 | 是否列入點名，預設為 `true` |

> 副檔名應為 `.json`，不是 `.josn`。

### 3.2 人員選取與搜尋

- 可用分類按鈕切換「全部」或指定分類。
- 顯示該分類的人員清單。
- 可用姓名或人員編號搜尋。
- 每位人員顯示目前狀態：正常、醫護所、轉診或全休。
- 已有事故的人員要有明顯但不刺眼的狀態標籤。

### 3.3 事故登記

事故類型第一版包含：

- 醫護所
- 轉診
- 全休

事故表單欄位：

| 欄位 | 必填 | 說明 |
| --- | --- | --- |
| 人員 | 是 | 從目前名單選擇 |
| 事故類型 | 是 | 醫護所、轉診或全休 |
| 開始時間 | 是 | 預設為目前時間 |
| 結束時間 | 視類型 | 用於判斷事故是否仍有效 |
| 備註 | 否 | 簡短說明，不放敏感醫療細節 |

時間規則：

- 勾選「全休」時，結束時間自動設定為開始日期當日的 `24:00`。
- 程式實際儲存為「次日 `00:00:00`」，並採用不含結束時間的區間：`startAt <= 現在 < endAt`。
- 例如 2026-08-09 09:00 登記全休，實際 `endAt` 為 2026-08-10 00:00。
- 系統時區固定使用 `Asia/Taipei`，前端顯示台灣時間，避免伺服器位於 UTC 時造成日期錯誤。
- 醫護所與轉診可手動填寫結束時間；若尚未確定，狀態維持有效，之後由使用者按「結束事故」。
- 事故時間結束後，該人員自動恢復正常並重新計入實到。
- 新增事故前，後端必須再次檢查該人員是否已有有效事故。

### 3.4 事故管理

- 查看目前事故清單。
- 依事故類型分組，並依各組人數由多至少排列。
- 每組內的人員可依開始時間由早到晚排列。
- 可修改事故類型、時間與備註。
- 可按「結束事故」，以目前時間寫入結束時間。
- 可查看歷史事故紀錄，並依日期、人員、分類及事故類型篩選。
- 刪除歷史資料屬高風險操作，第一版建議不提供；以「更正」或「作廢」保留紀錄。

### 3.5 即時更新

- 任一使用者新增、修改、結束事故，或匯入名單後，所有已開啟頁面都要自動更新。
- 第一版使用 **Server-Sent Events（SSE）**：後端主動通知前端重新取得摘要及清單。
- 前端斷線後自動重連；重連期間顯示「正在重新連線」。
- SSE 只負責通知資料已變更，實際資料仍透過 REST API 取得，邏輯較單純。

## 4. 頁面與介面設計

### 4.1 首頁版面

1. 頂端：系統名稱、目前日期時間、連線狀態。
2. 分類列：全部、入伍生、學生幹部、一至四年級。
3. 統計卡片：應到、事故合計、實到。
4. 事故卡片：依人數動態排序。
5. 人員清單：姓名、編號、分類、目前狀態與操作按鈕。
6. 事故登記視窗：選擇類型、時間與備註。

### 4.2 視覺風格

- 扁平化設計，避免過多陰影、漸層與動畫。
- 背景使用淺灰或米白色，卡片使用白色。
- 正常狀態使用藍綠色；事故狀態使用橘色或紅色，但要維持可讀性。
- 統計數字放大，讓值勤人員可以快速辨識。
- 手機、平板、電腦皆可操作，手機版按鈕高度至少 44px。
- 不可只靠顏色區分狀態，需同時顯示文字或圖示。
- 危險操作需二次確認。

## 5. 前後端架構

```mermaid
flowchart LR
    A[瀏覽器前端] -->|REST API| B[Express 後端]
    B -->|JSON 讀寫| C[資料檔案]
    B -->|SSE 更新通知| A
```

### 5.1 前端

- 原生 HTML5
- CSS3
- 原生 JavaScript（ES Modules）
- Fetch API 呼叫後端
- EventSource 接收 SSE 通知

### 5.2 後端

- Node.js 20 LTS 以上
- Express
- Zod：驗證 API 與匯入 JSON 格式
- Luxon：處理 `Asia/Taipei` 時區與全休結束時間
- `fs/promises`：第一版儲存 JSON 資料

### 5.3 為什麼先用這組技術

- 不需要 React、Vue 或前端打包工具，安裝項目少。
- 前後端仍完全分離，未來可單獨替換其中一端。
- Express 容易閱讀與維護。
- SSE 比 WebSocket 更適合這種「伺服器通知畫面更新」的單向需求。
- 適合單機、內網、小規模使用；若多人高頻操作，再升級 SQLite 或 PostgreSQL。

## 6. API 規格草案

所有 API 回傳 JSON，路徑統一加上 `/api`。

| 方法 | 路徑 | 功能 |
| --- | --- | --- |
| `GET` | `/api/categories` | 取得分類清單 |
| `GET` | `/api/people?category=&q=` | 取得與搜尋人員 |
| `POST` | `/api/people/import` | 匯入人員 JSON 檔 |
| `GET` | `/api/attendance/summary?category=` | 取得應到、事故與實到摘要 |
| `GET` | `/api/incidents/active?category=` | 取得目前有效事故 |
| `GET` | `/api/incidents/history` | 取得歷史事故 |
| `POST` | `/api/incidents` | 新增事故 |
| `PATCH` | `/api/incidents/:id` | 修改事故 |
| `POST` | `/api/incidents/:id/end` | 結束事故 |
| `POST` | `/api/incidents/:id/void` | 作廢錯誤紀錄 |
| `GET` | `/api/events` | 建立 SSE 連線 |
| `GET` | `/api/health` | 健康檢查 |

摘要回傳範例：

```json
{
  "category": "全部",
  "expected": 120,
  "incidentTotal": 8,
  "actual": 112,
  "incidents": [
    { "type": "全休", "count": 4 },
    { "type": "醫護所", "count": 3 },
    { "type": "轉診", "count": 1 }
  ],
  "updatedAt": "2026-08-09T15:30:00+08:00"
}
```

## 7. 資料結構

### 7.1 `people.json`

```json
{
  "version": 1,
  "people": []
}
```

### 7.2 `incidents.json`

```json
{
  "version": 1,
  "incidents": [
    {
      "id": "incident-uuid",
      "personId": "A001",
      "type": "全休",
      "startAt": "2026-08-09T09:00:00+08:00",
      "endAt": "2026-08-10T00:00:00+08:00",
      "note": "",
      "status": "active",
      "createdAt": "2026-08-09T08:55:00+08:00",
      "updatedAt": "2026-08-09T08:55:00+08:00"
    }
  ]
}
```

事故狀態建議使用：

- `active`：目前有效。
- `ended`：已正常結束。
- `voided`：資料輸入錯誤，保留紀錄但不計入統計。

## 8. 建議專案結構

```text
roll-call/
├─ frontend/
│  ├─ index.html
│  ├─ css/
│  │  └─ style.css
│  └─ js/
│     ├─ api.js
│     ├─ app.js
│     ├─ events.js
│     └─ ui.js
├─ backend/
│  ├─ package.json
│  ├─ src/
│  │  ├─ server.js
│  │  ├─ routes/
│  │  │  ├─ people.js
│  │  │  ├─ incidents.js
│  │  │  ├─ attendance.js
│  │  │  └─ events.js
│  │  ├─ services/
│  │  │  ├─ peopleService.js
│  │  │  ├─ incidentService.js
│  │  │  └─ attendanceService.js
│  │  ├─ validators/
│  │  │  └─ schemas.js
│  │  └─ utils/
│  │     ├─ fileStore.js
│  │     └─ time.js
│  └─ data/
│     ├─ people.json
│     └─ incidents.json
├─ samples/
│  └─ people.example.json
├─ .gitignore
├─ document.md
└─ README.md
```

## 9. 非功能需求

- 主要頁面操作後，正常內網環境應在 1 秒內顯示結果。
- 所有輸入都由後端再次驗證，不能只依賴前端驗證。
- JSON 寫入需採「暫存檔寫完後原子替換」，避免程式中斷造成檔案損毀。
- 同一時間的寫入需排隊，避免兩個請求互相覆蓋。
- API 錯誤回傳一致格式，前端顯示可理解的中文訊息。
- 後端紀錄操作時間、操作類型與錯誤，但避免在日誌寫入不必要的個人或醫療資訊。
- 每日備份資料檔，並定期測試能否還原。

## 10. 驗收條件

1. 能成功匯入符合格式的人員 JSON。
2. 格式錯誤、分類錯誤或人員編號重複時，能指出確切錯誤。
3. 切換分類後，應到、事故合計、實到及人員清單同步更新。
4. 新增事故後，其他已開啟的瀏覽器能自動更新。
5. 事故卡片會依人數由多至少排序。
6. 同一人不能同時建立兩筆有效事故。
7. 全休的結束時間會自動設為當日 24:00，且隔日 00:00 後不再列入事故。
8. `事故合計` 為不重複事故人數，且 `實到 = 應到 - 事故合計`。
9. 修改、結束或作廢事故後，統計立即正確更新。
10. 手機、平板與桌機皆能完成主要操作。
11. 伺服器重啟後，人員與事故資料仍存在。

## 11. 開發階段

### 第一階段：可用版本

- 建立前後端基本結構。
- JSON 人員匯入與驗證。
- 分類、人員清單與搜尋。
- 事故新增、結束與統計。
- 全休至當日 24:00 的時間規則。
- SSE 即時更新。
- 基本響應式扁平化介面。

### 第二階段：管理功能

- 事故修改、作廢與歷史查詢。
- 匯入預覽及錯誤報表。
- 操作紀錄、備份與還原。
- 權限與登入。

### 第三階段：正式部署強化

- 資料儲存改為 SQLite 或 PostgreSQL。
- HTTPS、帳號權限與稽核紀錄。
- 自動化測試與部署。

## 12. 需要注意的事項

1. **先確認實到算法**：本文件先定義所有有效事故都不算實到。如果「醫護所」仍算在營、或某些事故不應扣除，需替事故類型增加 `deductFromActual` 設定。
2. **JSON 不適合大量並行寫入**：小型單機或內網使用沒有問題；如果同時操作人數增加，建議改用 SQLite。
3. **不要直接公開到網際網路**：人員姓名與事故狀態屬敏感資料。正式上線前至少要有登入、權限、HTTPS、防火牆與備份。
4. **避免記錄醫療細節**：備註只寫管理所需資訊，不存放診斷、病歷或其他非必要個資。
5. **時區要固定**：伺服器即使使用 UTC，也必須用 `Asia/Taipei` 計算「當日 24:00」。
6. **匯入前要預覽**：大量名單直接覆蓋容易誤刪或誤改，建議顯示新增、更新、停用與錯誤筆數，確認後才套用。
7. **不要用姓名當唯一值**：可能有同名人員，必須使用穩定且唯一的 `id`。
8. **保留歷史紀錄**：事故資料建議作廢而非直接刪除，方便查核與更正。
9. **瀏覽器時間不可完全信任**：建立與修改時間以後端時間為準，前端只負責輸入及顯示。
10. **正式部署要限制檔案上傳**：只接受 JSON、限制大小，並驗證內容，避免任意檔案上傳風險。

## 13. 第一版暫定決策

若沒有其他補充，第一版先採以下設定：

- 全部有效事故都會從實到人數扣除。
- 三種事故類型為醫護所、轉診、全休。
- 人員分類固定為六種，但程式集中設定，方便日後擴充。
- 全休結束時間為開始日期當日 24:00。
- 使用台灣時區 `Asia/Taipei`。
- 使用 JSON 儲存資料，定位為單機或小型內網版本。
- 不含帳號登入；若要正式存放真實人員資料，上線前必須補上權限控管。
